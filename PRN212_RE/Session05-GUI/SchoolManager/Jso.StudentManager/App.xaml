<Application x:Class="Jso.StudentManager.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Jso.StudentManager"
             StartupUri="LoginWindow.xaml">
    <Application.Resources>
        <!-- Đoạn lệnh trong này là nơi khai báo CSS -->
        <Style
            TargetType="Button">
            <Setter
                Property="Background"
                Value="Black" />
            <Setter
                Property="Foreground"
                Value="White" />
            <Setter
                Property="FontSize"
                Value="16" />
        </Style>

        <Style 
            x:Key="Blue_Violet_Button"
            TargetType="Button">
            <Setter
                Property="Background"
                Value="BlueViolet" />
            <Setter
                Property="Foreground"
                Value="White" />
            <Setter
                Property="FontSize"
                Value="16" />
        </Style>

        <!-- Áp dụng đồng loạt mọi nhãn trên màn hình có màu chữ vàng, font size 20, đậm, nền trong suốt -->
        <Style TargetType="Label">
            <Setter 
                Property="Foreground"
                Value="Black" />

            <Setter
                Property="FontSize"
                Value="20" />

            <Setter
                Property="FontWeight"
                Value="bold" />

            <Setter
                Property="Background"
                Value="Transparent" />
        </Style>

        <!-- mọi ô nhập trên màn hình có màu chữ đỏ đậm, font size 20, đậm, nền trắng -->
        <Style
            TargetType="TextBox">
            <Setter
                Property="Foreground"
                Value="Black" />

            <Setter
                Property="FontSize"
                Value="20" />

            <Setter
                Property="FontWeight"
                Value="bold" />
        </Style>

    </Application.Resources>
</Application>
